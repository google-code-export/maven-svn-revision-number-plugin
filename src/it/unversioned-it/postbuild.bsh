try {
    return oe.maven.plugins.revision.IntegrationTestHelper.verifyResult(basedir, "r\\d+ \\?", "trunk/src/it/unversioned-it/src");
} catch (Throwable t) {
    t.printStackTrace();
    return false;
}
