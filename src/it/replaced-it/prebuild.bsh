import oe.maven.plugins.revision.IntegrationTestHelper;

try {
    boolean delete = IntegrationTestHelper.deleteFile( basedir, "src/test/resources/replaced.txt", true );
    boolean add = IntegrationTestHelper.addFile( basedir, "src/test/resources/replaced.txt", true );
    return delete && add;
} catch ( Throwable t ) {
    t.printStackTrace();
    return false;
}
