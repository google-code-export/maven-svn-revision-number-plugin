import oe.maven.plugins.revision.IntegrationTestHelper;

try {
    return IntegrationTestHelper.addFile( basedir, "src/test/resources/added.txt", true );
} catch ( Throwable t ) {
    t.printStackTrace();
    return false;
}
