import oe.maven.plugins.revision.IntegrationTestHelper;

try {
    boolean delete = IntegrationTestHelper.deleteFile( basedir, "src/test/resources/obstructed.txt", false );
    boolean add = IntegrationTestHelper.addDirectory( basedir, "src/test/resources/obstructed.txt", false );
    return delete && add;
} catch ( Throwable t ) {
    t.printStackTrace();
    return false;
}
